import * as carregarProdutos from "./Produtos.js";

//--------------------------------------------------
/*
async function conexaoSistema () {
  let conexao = true
  if(conexao){
    return `Produtos carregados com sucesso`
    //return Promise.resolve(`Produtos carregados com sucesso`)
  }
  else {
    throw `!!Problema ao carregar Produtos!!`
    //return Promise.reject(`!!Problema ao carregar produtos!!`)
  }
}
async function listarProdutos () {
  try {
    let msg = await conexaoSistema()
    console.log(msg)
    carregarProdutos.mostrarProdutos();
  }
  catch (erro) {
    console.log(erro)
  }
}
*/
